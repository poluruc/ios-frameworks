// This is a wrapper to establish the dependency between RealmSwift and Realm frameworks
// RealmSwift depends on Realm, so we need this wrapper target to express that dependency

@_exported import Realm
@_exported import RealmSwift
