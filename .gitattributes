# Git LFS Configuration for iOS Frameworks Repository

# =============================================================================
# Binary Framework Files
# =============================================================================

# XCFramework binaries (these can be large)
*.xcframework/** filter=lfs diff=lfs merge=lfs -text

# Individual framework binaries within XCFrameworks
*.xcframework/**/Alamofire filter=lfs diff=lfs merge=lfs -text
*.xcframework/**/Realm filter=lfs diff=lfs merge=lfs -text
*.xcframework/**/RealmSwift filter=lfs diff=lfs merge=lfs -text
*.xcframework/**/Kingfisher filter=lfs diff=lfs merge=lfs -text
*.xcframework/**/DGCharts filter=lfs diff=lfs merge=lfs -text
*.xcframework/**/JWTDecode filter=lfs diff=lfs merge=lfs -text

# Legacy framework format (if any exist)
*.framework/** filter=lfs diff=lfs merge=lfs -text

# Static libraries
*.a filter=lfs diff=lfs merge=lfs -text

# Dynamic libraries
*.dylib filter=lfs diff=lfs merge=lfs -text

# =============================================================================
# Code Signature and Metadata
# =============================================================================

# Code signature directories (can contain binary data)
*/_CodeSignature/** filter=lfs diff=lfs merge=lfs -text

# Compiled modules (binary content)
*.swiftmodule filter=lfs diff=lfs merge=lfs -text

# =============================================================================
# Archive and Distribution Files
# =============================================================================

# Archive files (if any are committed)
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.tar.bz2 filter=lfs diff=lfs merge=lfs -text

# Apple package files
*.pkg filter=lfs diff=lfs merge=lfs -text
*.dmg filter=lfs diff=lfs merge=lfs -text

# =============================================================================
# Text Files (NOT for LFS - handled by Git normally)
# =============================================================================

# These should NOT use LFS (they're text files):
# *.plist (text XML)
# *.xcprivacy (text XML) 
# *.md (markdown text)
# *.json (text JSON)
# *.txt (text)
# Info.plist files
# Headers/* directories (text source code)
# modulemap files (text)

# =============================================================================
# Documentation and Media (if added later)
# =============================================================================

# Images for documentation
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text
*.webp filter=lfs diff=lfs merge=lfs -text

# Video files (if any demos are added)
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.avi filter=lfs diff=lfs merge=lfs -text